[deploy]
preDeployCommand = "npx prisma migrate deploy"
startCommand = "node -e \"require('http').createServer((req,res)=>{res.end('ok')}).listen(process.env.PORT||3000,()=>console.log('listening on port',process.env.PORT||3000))\""
