[deploy]
preDeployCommand = "npx prisma migrate deploy"
startCommand = "echo '=== STARTING ===' && ls .next/standalone/ && node .next/standalone/server.js"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
